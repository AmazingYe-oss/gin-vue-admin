# 运行时状态：数据库、日志绝不进镜像
data/
log/
*.db

# 配置与密钥：运行时用 -v 挂载注入，不烧进镜像层
config.yaml
config.*.yaml

# 构建产物与镜像定义自身（避免无谓的 context 传输和缓存失效）
gva-server
server
Dockerfile
.dockerignore
docker-compose*.yml
Makefile

# 文档与开发杂物
# 注意：server/docs/ 是 swag 生成的 Swagger Go 代码包，router.go 有 import，绝不能排除
*.md
.idea/
.vscode/
.git/
